# v0.2.10

## 修复

- **图片与文件附件发送失败**。此前有两个独立原因：
  1. 模型目录中未声明图片能力时，模型会被当作纯文本模型，发送图片直接被拒；
  2. Android 上附件落盘过程中的目录同步调用会被系统拒绝，保存中断并报发送失败。
  现在部署时会检查模型声明并在缺失时给出提示，附件保存也已适配 Android。`deepseek-flash` 可直接发图。

## 升级

- 覆盖安装（versionCode 14 / v0.2.10），随后点一次「一键部署」让运行时补丁生效。

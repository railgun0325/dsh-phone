# v0.2.9

## 修复

- **输入栏「添加附件」按钮点击无反应**：WebView 需要宿主提供文件选择器才会响应文件输入，现已实现——点击后弹出系统选择器，文件以 `content://` 交给页面，不需要存储权限。
- **root 授权失败时提示更明确**：若超级用户管理器拒绝了本应用，会直接提示到 Magisk「超级用户」中打开开关，而不是笼统提示"未检测到 root"。
- 部署不再重复启动后台看门狗。

## 升级

- 覆盖安装（versionCode 13 / v0.2.9）。
